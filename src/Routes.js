import React from 'react';
import Layout from "./components/hoc/Layout";



const Routes = (props) => {
    return (
        <div>
           <Layout/>
        </div>
    );
};

export default Routes;